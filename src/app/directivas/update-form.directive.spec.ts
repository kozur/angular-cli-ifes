import { UpdateFormDirective } from './update-form.directive';

describe('UpdateFormDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateFormDirective();
    expect(directive).toBeTruthy();
  });
});
